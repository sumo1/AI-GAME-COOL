---
name: git-push
description: "智能 Git 提交与推送。当用户说 '提交'、'commit'、'push'、'git-push'、'推一下'、'提交代码' 时触发。执行完整的「文件审查 → 前置检查 → 独立代码审查 → 文档新鲜度检查 → 生成中文提交信息 → 推送」流程。"
user_invocable: true
version: "1.0.0"
---

# git-push: 智能提交与推送

读取并执行 `agents/ci-pre-checker/git-push.md` 中定义的完整流程。
